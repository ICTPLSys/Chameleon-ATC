Fair comparison requires the same hardware configuration for Chameleon and the baseline systems. We currently have too few servers with that configuration to host the baseline environments alongside the prepared Chameleon environment. We therefore provide pre-run baseline measurements for plotting together with reviewer-generated Chameleon results. Reviewers interested in running the baselines can contact the authors; we will set up their environments when additional matching servers become available.
